import { CLIEngine, Rule } from 'eslint';
import { CreateParser, Parser } from '../types/parsers';
declare class ESLintParser implements Parser<CLIEngine.LintReport> {
    engine: CLIEngine;
    rules: Map<string, Rule.RuleModule>;
    constructor(config: CLIEngine.Options);
    execute(paths: string[]): Promise<CLIEngine.LintReport>;
}
declare let createParser: CreateParser<CLIEngine.Options, Parser<CLIEngine.LintReport>>;
export { createParser, ESLintParser };
//# sourceMappingURL=eslint-parser.d.ts.map