"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_1 = require("../config");
class ActionsEvaluator {
    constructor() {
        this.actions = [];
    }
    add(action) {
        this.actions.push(action);
    }
    evaluate(config) {
        var _a;
        let actionConfig = (_a = config.actions) !== null && _a !== void 0 ? _a : {};
        return this.actions.filter((action) => {
            let [enabled, value] = config_1.parseConfigTuple(actionConfig[action.name]);
            let threshold = value && typeof value.threshold === 'number' ? value.threshold : action.defaultThreshold;
            return enabled && action.input >= threshold;
        });
    }
}
exports.default = ActionsEvaluator;
//# sourceMappingURL=actions-evaluator.js.map