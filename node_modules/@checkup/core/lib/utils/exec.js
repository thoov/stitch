"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.exec = void 0;
const util = require('util');
const execAsPromise = util.promisify(require('child_process').exec);
/**
 * @param cmd
 * @param options
 * @param defaultValue
 * @param toType
 */
async function exec(cmd, options, defaultValue, toType = String) {
    const { stdout } = await execAsPromise(cmd, options);
    return toType(stdout.trim()) || defaultValue;
}
exports.exec = exec;
//# sourceMappingURL=exec.js.map