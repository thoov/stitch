import BaseTask from './base-task';
import { TaskContext } from './types/tasks';
export default abstract class BaseValidationTask extends BaseTask {
    private validationSteps;
    constructor(pluginName: string, context: TaskContext);
    protected addValidationStep(description: string, validate: () => boolean): void;
    protected validate(): Map<string, boolean>;
}
//# sourceMappingURL=base-validation-task.d.ts.map