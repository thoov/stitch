export default class AstTraverser<TAst, TVisitors, TParse extends (source: string, parserOptions?: any) => TAst, TTraverse extends (ast: TAst, visitors: TVisitors) => any> {
    source: string;
    private parser;
    private traverser;
    private parserOptions;
    ast: TAst;
    constructor(source: string, parser: TParse, traverser: TTraverse, parserOptions?: object);
    private _parse;
    traverse(visitors: TVisitors): this;
}
//# sourceMappingURL=ast-traverser.d.ts.map