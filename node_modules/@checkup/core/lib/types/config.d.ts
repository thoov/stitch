export declare type ConfigValue<T extends {}> = 'on' | 'off' | ['on' | 'off', T];
export declare type ActionConfig = Record<string, ConfigValue<{
    threshold: number;
}>>;
export declare type TaskConfig = {
    actions?: ActionConfig;
    [key: string]: any;
};
export declare type CheckupConfig = {
    $schema: string;
    excludePaths: string[];
    plugins: string[];
    tasks: Record<string, ConfigValue<TaskConfig>>;
};
//# sourceMappingURL=config.d.ts.map