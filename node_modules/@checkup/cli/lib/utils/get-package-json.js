"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPackageJson = void 0;
const fs_extra_1 = require("fs-extra");
const path_1 = require("path");
/**
 * @param basePath
 */
function getPackageJson(basePath, pathName = 'package.json') {
    let package_ = {};
    let packageJsonPath = path_1.join(path_1.resolve(basePath), pathName);
    try {
        package_ = fs_extra_1.readJsonSync(packageJsonPath);
    }
    catch (error) {
        if (error.code === 'ENOENT') {
            throw new Error(`The ${path_1.resolve(basePath)} directory found through the 'path' option does not contain a package.json file. You must run checkup in a directory with a package.json file.`);
        }
    }
    return package_;
}
exports.getPackageJson = getPackageJson;
//# sourceMappingURL=get-package-json.js.map